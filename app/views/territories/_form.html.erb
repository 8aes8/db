<%= form_for(@territory) do |f| %>
  <% if @territory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@territory.errors.count, "error") %> prohibited this territory from being saved:</h2>

      <ul>
      <% @territory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :territory_description %><br>
    <%= f.text_field :territory_description %>
  </div>
  <div class="field">
    <%= f.label :region_id %><br>
    <%= f.text_field :region_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
